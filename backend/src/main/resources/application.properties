spring.application.name=mutualfundcalc

# ── Database (disabled until persistence phase) ──────────────────────────────
# Prevents startup crash: JPA is in pom.xml but no DB URL is configured yet.
# Remove these exclusions when you add a real datasource later.
spring.autoconfigure.exclude=\
  org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration,\
  org.springframework.boot.autoconfigure.orm.jpa.HibernateJpaAutoConfiguration

# spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/mutualfundcalc}
# spring.datasource.username=${DB_USER:postgres}
# spring.datasource.password=${DB_PASSWORD:postgres}
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.open-in-view=false

# ── Server ────────────────────────────────────────────────────────────────────
server.port=8080